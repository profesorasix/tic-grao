---
- name: update packages
  hosts: myhosts

  tasks:
  - name: Update packages
    ansible.builtin.apt:
      state: latest
      pkg:
      - sublime-text
      - vagrant
      - incus
      update_cache: yes      